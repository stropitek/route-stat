'use strict';

const properties = {
    cumulDistance: 'length',
    cumulDuration: 'time',
    distance: 'length',
    duration: 'time',
    elevation: 'length',
    cumulElevation: 'length',
    speed: 'speed'
};

module.exports = {properties};